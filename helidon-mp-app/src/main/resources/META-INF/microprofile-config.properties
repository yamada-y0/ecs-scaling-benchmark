# Microprofile server properties
server.port=8080
server.host=0.0.0.0

# Change the following to true to enable the optional MicroProfile Metrics REST.request metrics
metrics.rest-request.enabled=false

# Application properties. This is the default greeting
# Datasource properties
javax.sql.DataSource.ds1.dataSourceClassName=org.postgresql.ds.PGSimpleDataSource
javax.sql.DataSource.ds1.dataSource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
javax.sql.DataSource.ds1.dataSource.user=${DB_USERNAME:postgres}
javax.sql.DataSource.ds1.dataSource.password=${DB_PASSWORD:postgres}
